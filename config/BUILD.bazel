load("//tools-bazel:library_wrappers.bzl", "java_export", "jvm_junit_test_suite")

java_export(
    name = "config",
    srcs = glob(["src/main/java/**/*.java"]),
    maven_coordinates = "io.confluent:common-config:$(maven_version)",
    visibility = ["//visibility:public"],
    deps = [
        "//utils",
        "@maven//:org_slf4j_slf4j_api",
    ],
)

jvm_junit_test_suite(
    name = "config-tests",
    srcs = glob(["src/test/java/**/*.java"]),
    # ConfigDefTest.testClassLoading reads target/classes/ (cwd-relative). Stage
    # ConfigUtils.class there via the root genrule.
    data = ["//:configutils_at_target_classes"],
    runtime_deps = [
        "@maven//:org_junit_jupiter_junit_jupiter_engine",
        "@maven//:org_junit_platform_junit_platform_launcher",
        "@maven//:org_junit_platform_junit_platform_reporting",
        "@maven//:org_junit_vintage_junit_vintage_engine",
    ],
    deps = [
        ":config",
        "@maven//:javax_xml_bind_jaxb_api",
        "@maven//:junit_junit",
    ],
)
