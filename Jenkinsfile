#!/usr/bin/env groovy

common {
    slackChannel = '#kafka-warn'
    downStreamRepos = ["rest-utils", "ksql", "newwave", "hub-client", "kafka-connect-jdbc"]
    nanoVersion = true
    timeoutHours = 3
}
